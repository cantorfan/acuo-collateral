allprojects {
    repositories {
        jcenter()
    }
    apply plugin: 'java'        
    apply plugin: 'jacoco'
    apply plugin: 'project-report'    
    group = 'com.acuo.collateral'
    version = '1.0'
}